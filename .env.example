GOOGLE_MAPS_API_KEY=your_key_here

# Safety cap on categories x locations per scrape run (default 400 if unset).
# Raise only when you deliberately want a bigger, more expensive run.
MAX_QUERIES_PER_RUN=400

# Optional: only needed if you turn on local AI enrichment via Ollama.
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=llama3.1
