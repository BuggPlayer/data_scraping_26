1	python3 lead_generator.py

Set your API key in `.env` (copy `.env.example` to `.env`) — do not paste it here or hardcode it in scripts.

⚠️ The key that used to be pasted in this file (starting AIzaSyB6QU...) was exposed in plaintext. Rotate/delete it in Google Cloud Console immediately and replace it with a new one in `.env`.
